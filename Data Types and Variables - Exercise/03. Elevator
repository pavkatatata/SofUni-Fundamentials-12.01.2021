using System;

namespace ConsoleApp7
{
    class Program
    {
        static void Main(string[] args)
        {

            int n = int.Parse(Console.ReadLine());
            int p = int.Parse(Console.ReadLine());

            var courses = (double)n / p;
            Console.WriteLine(Math.Ceiling(courses));

        }
    }
}
